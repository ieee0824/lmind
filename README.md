# lmind

複数のローカルLLMを脳の部位に見立て、常時思考ループを回すことで「思考」を実現する実験的プロジェクト。

## 仮説

- LLMの単発出力は思考ではない
- LLMの出力をループさせた上で、間に発生する中間情報が思考である

## アーキテクチャ

```
┌──────────┐  ┌──────────┐  ┌────────────┐
│ frontal  │←→│ temporal  │←→│hippocampus │
│ 前頭葉   │  │ 側頭葉    │  │ 海馬       │
│ gemma3:4b│  │ gemma3:1b │  │ gemma3:1b  │
│ 推論/判断│  │ 連想/認識 │  │ 記憶/想起  │
└────┬─────┘  └─────┬─────┘  └─────┬──────┘
     │              │              │
     └──────────────┼──────────────┘
                    │
            ┌───────▼───────┐
            │  思考バス      │
            │ (Go channels) │
            └───────┬───────┘
                    │
            ┌───────▼───────┐
            │ モード判定     │  ← gemma3:1b で高速分類
            │ chat / task   │
            └───────┬───────┘
                    │
            ┌───────▼───────┐
            │  Broca 野     │  ← 発話の起案（モード別プロンプト）
            │  (言語出力)    │
            └───────┬───────┘
                    │
            ┌───────▼───────┐
            │  抑制ゲート    │  ← コード: 地の文除去
            │  code + LLM   │     LLM: 要約（長い場合のみ）
            └───────┬───────┘     差が大きければ差し戻し（最大2回）
                    │
            ┌───────▼───────┐
            │   Chat I/F    │
            └───────────────┘
```

各脳部位はgoroutineで独立に動作し、思考バス経由で中間情報（＝思考）を交換し続ける。

ユーザー応答時はOllama渋滞を防ぐため、自律思考への入力配信を応答完了後まで遅延する。

### 応答パイプライン

1. **モード判定** — gemma3:1b がユーザー入力を `chat`（雑談）か `task`（秘書）に分類
2. **Broca起案** — モードに応じたプロンプトで gemma3:4b が返答を起案
3. **地の文除去** — 正規表現でト書き・装飾・かぎかっこを機械的に除去
4. **要約** — 150文字超の場合のみ LLM で1〜3文に要約
5. **差し戻し判定** — 起案と整形後の長さ比が0.5以下なら理由付きで差し戻し（最大2ラウンド）

## 性格設定

`~/.lmind/char_setting.md` に性格設定ファイルを配置する。各脳部位に以下のように配分される：

| 部位 | 参照する設定 |
|------|-------------|
| 前頭葉 | 思考の行動指針（判断傾向・介入の強さ） |
| 側頭葉 | 人格・雰囲気（連想の方向性・感受性） |
| Broca野（chat） | 全体（友人として会話、1〜2文） |
| Broca野（task） | 全体（AI秘書として実務支援、1〜5文） |
| 抑制ゲート | コード: 地の文除去 / LLM: 要約のみ |

## 依存

- [Ollama](https://ollama.ai) — ローカルLLM実行環境
- [memAI-go](https://github.com/ieee0824/memAI-go) — 脳科学インスパイアの記憶システム（STM/LTM/感情検出）

## セットアップ

```bash
ollama pull gemma3:4b
ollama pull gemma3:1b
cp char_setting.md ~/.lmind/char_setting.md
go build -o lmind .
./lmind
```

## 使い方

起動すると各脳部位が自律的に思考を開始する。CLIから対話可能。

```
lmind chat (type 'quit' to exit, 'thoughts' to see recent thinking)
> こんにちは        # chatモードで応答
> 明日の予定教えて   # taskモードで応答
> thoughts          # 内部の思考状態を表示
> quit              # 終了
```

思考・起案・抑制評価のログは `~/.lmind/thoughts.db` に記録される（1週間で自動削除）。
